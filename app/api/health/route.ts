import { NextResponse } from "next/server";
export function GET() { const demo = process.env.DEMO_MODE === "true"; const supabase = Boolean(process.env.NEXT_PUBLIC_SUPABASE_URL && (process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY || process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY)); return NextResponse.json({ ok: true, service: "inquiry-desk", mode: process.env.EXTRACTION_PROVIDER === "openai" && process.env.OPENAI_API_KEY ? "provider" : "deterministic", persistence: demo ? "demo-memory" : supabase ? "supabase-auth-postgres" : "not-configured", at: new Date().toISOString() }); }
