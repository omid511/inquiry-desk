"use client";
import { useState } from "react";
export default function LoginForm() { const [error, setError] = useState(""); const [busy, setBusy] = useState(false); return <form onSubmit={async (event) => { event.preventDefault(); setBusy(true); setError(""); const password = new FormData(event.currentTarget).get("password"); const response = await fetch("/api/auth/login", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ password }) }); if (response.ok) window.location.href = "/owner"; else { setError("That access code was not accepted."); setBusy(false); } }}><div className="field"><label htmlFor="password">Access code</label><input autoFocus id="password" name="password" type="password" required /></div><button className="button" disabled={busy}>{busy ? "Opening…" : "Enter desk →"}</button>{error && <p className="error" role="alert">{error}</p>}</form>; }
